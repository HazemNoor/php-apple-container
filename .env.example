# App — container/image/volume names derive from APP_NAME-APP_ENV
APP_NAME=app
APP_ENV=dev
APP_DOMAIN=apple-container.localhost
PMA_DOMAIN=phpmyadmin.localhost

# MySQL (DB_PASS is also the root password)
DB_NAME=app
DB_USER=app
DB_PASS=secret
