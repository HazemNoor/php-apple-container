{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug (app-dev-fpm)",
            "type": "php",
            "request": "launch",
            "hostname": "0.0.0.0",
            "port": 9003,
            "pathMappings": {
                "/var/www/html/public": "${workspaceFolder}/public"
            }
        }
    ]
}
